// import { SOME_ACTION_NAME } from "./types";
const initialState = {

};

export default (state = initialState, action) => {
  switch (action.type) {

  //   case SOME_ACTION_NAME:
  //     return { ...state }

  default:
    return state;
  }
};
